# AGENTS.md — 本仓库的操作说明书

> **任何 AI 工具（Claude Code / Cursor / Copilot / Codex / WorkBuddy / Cline …）在动这个仓库之前，先读这一份。**
> 这份文件是这个仓库的契约：它规定**怎么写调研 Skill**、**怎么放文件**、**什么算合格**、**什么禁止做**。
> `CLAUDE.md`、`.cursor/rules/`、`.github/copilot-instructions.md` 都只是指向这里，内容以本文件为准。

---

## 1. 这个仓库是什么

一组**调研 Skill** 的合集。每个 Skill 教 AI 怎么把一类「说不清的问题」变成**有证据、有判决**的结论。

| Skill | 覆盖领域 | 心脏（该领域的专属步骤） |
|---|---|---|
| `app-market-research` | App / 小程序立项：市场 → 竞品 → 三端合规 → 做不做 | **PLATFORM 三端硬约束七查** |

**为什么调研要写成 Skill，而不是写在提示词里**：不落成规则，AI 每次都会给出「这个方向很有前景，建议进一步调研」这类和稀泥结论。调研的价值不在信息量，在**判决**。

---

## 2. 四条继承铁律（写任何新 Skill 都必须继承，不许打折扣）

1. **判决必须落地**。禁止以「值得考虑」「看情况」「建议进一步调研」收尾。每个判决必须落到固定选项集合中的某一个，并附三件套：**最值钱的一步 + 最便宜的验证（含时间预算）+ 放弃信号**。
2. **证据分级**。①权威 = 官方/监管/原始数据源；②专业 = 行业报告、平台官方文档；③二手 = 内容农场、代办机构、SEO 博客（**只能当线索，不得当结论**）。报告里必须标明每条结论的等级。
3. **未取证 ≠ 没问题**。查不到就写「未取证」并计入风险。禁止用记忆里的数字冒充检索结果。
4. **反谄媚**。「别做」和「先验证」是常态判决之一。用户的热情不得影响结论——预算该听证据的。

---

## 3. 目录约定

```
AGENTS.md                          本文件：仓库契约（唯一真相源）
README.md                          给人看：是什么、有什么、怎么用
docs/SKILL-AUTHORING.md            怎么写一个调研 Skill（方法论）
docs/lessons-learned.md            实证记录：踩过的坑（按时间追加，不删旧条目）
skills/<name>/SKILL.md             每个 Skill 的入口
skills/<name>/references/*.md      该 Skill 的深度资料（清单、字段表、检索模板）
skills/<name>/examples/*.md        该 Skill 的真实产出样例（至少一份）
skills/_template/SKILL.md          新 Skill 的空模板（复制即用）
```

**命名**：`<领域>-research`，全小写连字符。例：`app-market-research`、`game-research`、`career-research`。
**禁止**：把多个领域的调研塞进同一个 Skill；领域不同 = 心脏步骤不同 = 必须分开。

---

## 4. 新增一个调研 Skill 的标准流程

1. **定领域边界**：写清这个 Skill「用」与「不用」的分界，并说明什么情况下应该换用别的 Skill（避免与既有 Skill 抢活）。
2. **找到这个领域的「心脏」**：本领域里，**最容易被偷工、又最决定成败的那一步**是什么？
   - App 立项的心脏 = 三端合规七查（因为上架审核 / 资质备案 / 平台分成能直接否定一个市场很大的想法）
   - 找不到心脏就别加这个 Skill —— 说明它只是通用调研的换皮。
3. **写 SKILL.md**：按 `skills/_template/SKILL.md` 的骨架填，不得省略「红线」「常见合理化借口」「验证清单」三节。
4. **配 references/**：把长清单、字段表、检索语句模板放这里。**禁止写死会变动的数字**（政策、价格、比例），只给查证入口 + 要求现场取证。
5. **跑一个真实 case**：拿一个真实问题完整跑一遍，产出物放进 `skills/<name>/examples/`。**没有真实样例的 Skill 不算完成。**
6. **自检**：过一遍第 6 节的清单，再提交。

---

## 5. SKILL.md 的结构要求

frontmatter 必需字段：`name`、`description`（含触发词，中文优先）、`when_to_use`、`version`、`license: MIT`。

正文必需小节（顺序固定）：

```
# 标题——一句主张（例：端决定命运，举证责任在调研方）
## 何时用 / 何时不用
## 硬信念（几条，违反即报告作废）
## 流程（编号步骤 + 每步的产出物 + 检索预算）
## 输出格式（可直接复制的 markdown 骨架）
## 红线
## 常见合理化借口（两列表：借口 | 现实）
## 与其他 Skill 的关系（谁管什么、谁不碰什么 + 接力顺序）
## 验证清单（可勾选）
```

**写作规范**：
- 中文正文；术语可保留英文但首次出现要解释。
- 能用表格就不用段落；步骤必须给出**产出物**，不能只说「进行分析」。
- 每条规则尽量带一句「为什么」——AI 遵守有理由的规则，比遵守纯命令的规则稳定得多。
- 给检索预算（上限次数或时间），并**指定预算里必须有多少投给最容易偷工的那一步**。

---

## 6. 提交前自检清单

- [ ] 判决集合是封闭的（不许「其他」），且每个判决都要求三件套
- [ ] 所有外部结论都标了证据等级；二手来源没有被当结论用
- [ ] 没有写死会变动的数字；变动项都转为「现场取证 + 附链接日期」
- [ ] 有真实 example，且 example 里有明确判决
- [ ] 「何时不用」写清了与其他 Skill 的分界
- [ ] 「常见合理化借口」至少 5 条，且每条都真的反驳得掉
- [ ] 验证清单可勾选、可证伪

## 7. 禁止做的事

- ❌ 往仓库里放**未经脱敏的真实客户/公司数据**（调研报告可以做例子，但要先确认无敏感性）
- ❌ 把「值得考虑」「视情况而定」写进任何输出模板
- ❌ 用记忆中的政策/价格/统计数字充当检索结果
- ❌ 新增与既有 Skill 职责重叠的 Skill（应先修改既有 Skill）
- ❌ 删除 `docs/lessons-learned.md` 中的旧条目（只追加、不复写）

---

## 8. 维护约定

- **真相源**：本仓库是这些调研 Skill 的唯一真相源。`tataCrayon/app-market-research` 是从这里同步出去的**单技能分发版**；改完这里要记得同步过去（见 README 的「同步」一节）。
- **提交信息**：`<type>: <中文简述>`，type 取 `feat` / `fix` / `docs` / `skill`（新增 Skill 用 `skill`）。
- **一本账**：每次新增或实质修改 Skill，往 `docs/lessons-learned.md` 追加一条实证。

---

## 9. 怎么在这个仓库里开始工作

```bash
# 克隆后，多数 AI 工具会自动读 AGENTS.md / CLAUDE.md / .cursor/rules
git clone https://github.com/tataCrayon/market-research.git
```

- **要新增一个调研 Skill** → 读 `docs/SKILL-AUTHORING.md`，复制 `skills/_template/`。
- **要用现成的 Skill** → 读 `skills/<name>/SKILL.md`，或把它装进你所用工具的技能目录（见 README）。
- **要改已有 Skill 的行为** → 先读它文末的「与其他 Skill 的关系」，确认没有越界改写别人的职责。
