---
description: market-research 仓库契约：怎么写调研 Skill、什么算合格、什么禁止做
globs:
alwaysApply: true
---

# market-research 仓库规则

**完整契约见根目录 `AGENTS.md`，本文件是 Cursor 的常驻摘要。**

## 仓库是什么

一组**调研 Skill** 的合集。每个 Skill 教 AI 把「说不清的问题」变成**有证据、有判决**的结论。
调研的价值不在信息量，在判决——没有规则约束，AI 会给出 20 条链接和一句「建议进一步调研」。

## 四条不许打折的铁律

1. **判决必须落地**：禁止「值得考虑」「视情况而定」。判决必须落到封闭选项集合中的一个，并附
   **最值钱的一步 + 最便宜的验证（含时间预算）+ 放弃信号**。
2. **证据分级**：①权威（监管/官方/原始数据源）②专业（行业报告、平台官方文档）③二手（内容农场、代办机构、SEO 博客）。
   二手只能当线索，不得当结论。
3. **未取证 ≠ 没问题**：查不到写「未取证」并计入风险；禁止用记忆中的数字充当检索结果。
4. **反谄媚**：「别做」「先验证」是常态判决之一，用户的热情不得影响结论。

## 做修改时

- 新增 Skill：复制 `skills/_template/` → `skills/<领域>-research/`，按 `docs/SKILL-AUTHORING.md` 填写，
  **必须跑一个真实 case 并把产出放进 `examples/`**（没有真实样例不算完成）。
- 改动前读 `AGENTS.md` 第 6 节自检清单与第 7 节禁止事项。
- 命名：`<领域>-research`，全小写连字符；一个领域一个 Skill，不要把多个领域塞进同一个。
- 目录：`SKILL.md` + `references/`（长清单、字段表、检索模板）+ `examples/`（真实产出）。
- **不要写死会变动的数字**（政策、价格、比例）：只给查证入口 + 要求现场取证并标注日期。
- `docs/lessons-learned.md` **只追加，不改写旧条目**。
- 提交信息：`<type>: <中文简述>`，type ∈ `feat` / `fix` / `docs` / `skill`。

## 唯一的真相源

本仓库是这些调研 Skill 的唯一真相源；`tataCrayon/app-market-research` 是同步出去的单技能分发版。
